<view:ProductImage xmlns:js="js.core" xmlns:view="sprd.view">

    <js:Script>

        <![CDATA[
        (function () {

            return {

                $classAttributes: ["article"],

                _commitChangedAttributes: function($) {
                    if ($ && $.article) {
                        var self = this;
                        $.article.product(function(err, product) {
                            self.set('product', product);
                        });
                    }
                },

                alt: function () {
                    if (this.$.article) {
                        return this.$.article.$.name;
                    }

                    return "";
                }

            }
        })
        ]]>

    </js:Script>
</view:ProductImage>