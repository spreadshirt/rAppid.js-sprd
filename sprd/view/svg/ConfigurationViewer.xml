<sprdSvg:ConfigurationViewerClass xmlns="http://www.w3.org/1999/xhtml"
                                  xmlns:svg="js.svg"
                                  xmlns:sprdSvg="sprd.view.svg" class="{errorClass()}" cid="cv">

    <svg:circle class="rotation-circle" cx="{cv.rotationX}" cy="{cv.rotationY}" r="{_rotationRadius}" visible="{isRotating()}"/>

    <svg:g cid="_assetContainer" scaleX="{_scale.x}" scaleY="{_scale.y}">
    </svg:g>


    <svg:circle class="rotation-center" cx="{cv.rotationX}" cy="{cv.rotationY}" r="{pixelToViewBox(2)}" visible="{isRotating()}"/>

    <svg:g translateX="{flipOffsetX()}" translateY="{flipOffsetY()}">
        <svg:rect class="configuration-boundary"
                  visible="{isSelectedConfiguration()}"
                  x="0" y="0"
                  width="{_configurationWidth}" height="{_configurationHeight}"/>

        <svg:rect cid="_scaleHandle" class="configuration-handle scale"
                  visible="{isScalable()}"
                  x="{substract(_configurationWidth,pixelToViewBox(_handleOffset))}" y="{substract(_configurationHeight,pixelToViewBox(_handleOffset))}"
                  width="{pixelToViewBox(_handleWidth)}" height="{pixelToViewBox(_handleWidth)}"/>

        <svg:use visible="{isScalable()}"
                 href="{baseUrl('sprd/css/library.svg#scale')}" scaleX="{pixelToViewBox(1)}" scaleY="{scaleX}"
                 translateX="{substract(_configurationWidth,pixelToViewBox(_handleOffset))}" translateY="{substract(_configurationHeight,pixelToViewBox(_handleOffset))}"
                 style="pointer-events: none"/>

        <svg:rect cid="_rotateHandle" class="configuration-handle rotate"
                  visible="{isRotatable()}"
                  x="{substract(_configurationWidth,pixelToViewBox(_handleOffset))}" y="{pixelToViewBox(_handle-Offset)}"
                  width="{pixelToViewBox(_handleWidth)}" height="{pixelToViewBox(_handleWidth)}"/>

        <svg:use visible="{isRotatable()}"
                 href="{baseUrl('sprd/css/library.svg#rotate')}" scaleX="{pixelToViewBox(1)}" scaleY="{scaleX}"
                 translateX="{_rotateHandle.x}" translateY="{_rotateHandle.y}"
                 style="pointer-events: none"/>


        <svg:rect cid="_deleteHandle" class="configuration-handle delete"
                  visible="{isRemovable()}"
                  onclick="deleteConfiguration"
                  x="{pixelToViewBox(_handle-Offset)}" y="{substract(_configurationHeight,pixelToViewBox(_handleOffset))}"
                  width="{pixelToViewBox(_handleWidth)}" height="{pixelToViewBox(_handleWidth)}"/>

        <svg:use visible="{isRemovable()}"
                 href="{baseUrl('sprd/css/library.svg#remove')}" scaleX="{pixelToViewBox(1)}" scaleY="{scaleX}"
                 translateX="{_deleteHandle.x}" translateY="{_deleteHandle.y}"
                 style="pointer-events: none"/>

    </svg:g>


</sprdSvg:ConfigurationViewerClass>